<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>#(title)</title>
    <script src="https://unpkg.com/htmx.org@2.0.2"></script>
    <link rel="stylesheet" href="/styles/app.generated.css" />
  </head>
  <body class="flex flex-col min-h-screen bg-gray-100">
    <main class="flex items-center justify-center flex-grow">
      <div class="overflow-hidden rounded-lg bg-white shadow-md">
        <div class="px-4 py-5 sm:p-6">
          <div class="flex flex-col items-center space-y-6">
            <p class="text-lg leading-8 text-gray-600 text-center">#(title)</p>
            <form hx-post="/start-process" hx-target="#output" class="space-y-6">
              <div class="flex justify-center">
                <button type="submit" class="px-4 py-2 bg-blue-600 text-white font-semibold rounded-lg shadow-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2">Start Process</button>
              </div>
            </form>
          </div>
        </div>
      </div>
    </main>
  </body>
</html>
